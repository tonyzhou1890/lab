import Schema from '../service-schema'

const schema = new Schema({
  code: 'BaseConvert',
  icon: 'svguse:#icon-base-convert',
  i18nKey: 'baseConvert',
  categories: ['dev'],
})

export default schema
