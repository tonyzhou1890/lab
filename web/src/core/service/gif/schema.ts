import Schema from '../service-schema'

const schema = new Schema({
  code: 'Gif',
  icon: 'svguse:#icon-gif',
  i18nKey: 'gif',
  link: 'https://spark-gif.eachthought.com/',
  categories: ['image'],
})

export default schema
