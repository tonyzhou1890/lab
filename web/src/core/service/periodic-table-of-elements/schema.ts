import Schema from '../service-schema'

const schema = new Schema({
  code: 'PeriodicTableOfElements',
  icon: 'svguse:#icon-elements',
  i18nKey: 'periodicTableOfElements',
  categories: ['reference'],
})

export default schema
