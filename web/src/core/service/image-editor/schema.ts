import Schema from '../service-schema'

const schema = new Schema({
  code: 'ImageEditor',
  name: 'imageEditor.title',
  desc: 'imageEditor.desc',
  keywords: 'imageEditor.keywords',
  categories: ['global.category.image'],
})

export default schema
