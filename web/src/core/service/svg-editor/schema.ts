import Schema from '../service-schema'

const schema = new Schema({
  code: 'SVGEditor',
  icon: 'svguse:#icon-svg',
  i18nKey: 'svgEditor',
  categories: ['image'],
  link: 'https://svg.dowhat.top',
})

export default schema
