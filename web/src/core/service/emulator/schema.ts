import Schema from '../service-schema'

const schema = new Schema({
  code: 'Emulator',
  icon: 'videogame_asset',
  i18nKey: 'emulator',
  categories: ['game'],
  stage: 'Beta',
  infoIcon: true,
})

export default schema
